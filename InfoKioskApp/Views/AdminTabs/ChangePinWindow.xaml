<UserControl x:Class="InfoKioskApp.Views.AdminTabs.ChangePinView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1e1e1e">

    <Border Background="#2b2b2b"
            CornerRadius="12"
            Padding="20"
            Margin="15"
            BorderBrush="#3a3a3a"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Смена PIN-кода администратора"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="0,0,0,10"/>

            <StackPanel Grid.Row="1" Margin="0,6">
                <TextBlock Text="Текущий PIN:" Foreground="White"/>
                <PasswordBox x:Name="OldPinBox" Background="#333" Foreground="White" Margin="0,6,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,6">
                <TextBlock Text="Новый PIN:" Foreground="White"/>
                <PasswordBox x:Name="NewPinBox" Background="#333" Foreground="White" Margin="0,6,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Margin="0,6">
                <TextBlock Text="Повторите новый PIN:" Foreground="White"/>
                <PasswordBox x:Name="ConfirmPinBox" Background="#333" Foreground="White" Margin="0,6,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
                <Button Content="💾 Сохранить" Width="130" Height="40" Margin="6"
                        Background="#3A6DF0" Foreground="White" FontWeight="Bold"
                        Click="Save_Click"/>
                <Button Content="Отмена" Width="100" Height="40" Margin="6"
                        Background="#555" Foreground="White"
                        Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>


