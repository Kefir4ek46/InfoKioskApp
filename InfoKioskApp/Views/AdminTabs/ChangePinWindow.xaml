<UserControl x:Class="InfoKioskApp.Views.AdminTabs.ChangePinView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#20222A">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#1F2129"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#343848"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Height" Value="34"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#262933"
            CornerRadius="12"
            Padding="20"
            Margin="16"
            BorderBrush="#343848"
            BorderThickness="1">
        <StackPanel>
            <TextBlock Text="Сменить PIN-код"
                       FontSize="22"
                       FontWeight="Bold"
                       Margin="0,0,0,16"/>

            <TextBlock Text="Старый PIN:"/>
            <PasswordBox x:Name="OldPinBox" Margin="0,6,0,12"/>

            <TextBlock Text="Новый PIN:"/>
            <PasswordBox x:Name="NewPinBox" Margin="0,6,0,12"/>

            <TextBlock Text="Подтверждение PIN:"/>
            <PasswordBox x:Name="ConfirmPinBox" Margin="0,6,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="💾 Сохранить"
                        Width="160" Height="36" Margin="0,0,10,0"
                        Background="#3A6DF0" Foreground="White" FontWeight="SemiBold"
                        BorderThickness="0"
                        Click="Save_Click"/>
                <Button Content="Отмена"
                        Width="130" Height="36"
                        Background="#4A4F5F" Foreground="White"
                        BorderThickness="0"
                        Click="Cancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
