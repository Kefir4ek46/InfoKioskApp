<UserControl x:Class="InfoKioskApp.Views.ChangesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="#111">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Изменения в расписании"
                   FontSize="26"
                   Foreground="White"
                   Margin="20,10,0,10"
                   FontWeight="Bold"/>

        <!-- Excel -->
        <ScrollViewer x:Name="ExcelContainer" Grid.Row="1" Margin="20" Visibility="Collapsed">
            <DataGrid x:Name="ChangesTable"
                      AutoGenerateColumns="True"
                      Background="#222"
                      Foreground="White"
                      AlternatingRowBackground="#1a1a1a"
                      HeadersVisibility="All"
                      IsReadOnly="True"/>
        </ScrollViewer>

        <!-- Image -->
        <ScrollViewer x:Name="ImageContainer" Grid.Row="1" Margin="20" Visibility="Collapsed">
            <Image x:Name="ChangesImage" Stretch="Uniform" />
        </ScrollViewer>

    </Grid>
</UserControl>
