<UserControl x:Class="InfoKioskApp.Views.RemoteSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Настройки удалённого подключения" 
                       FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="IP адрес сервера:"/>
            <TextBox x:Name="IpAddressBox" Width="200" Margin="0,5,0,10" Text="127.0.0.1"/>

            <TextBlock Text="Порт:"/>
            <TextBox x:Name="PortBox" Width="100" Margin="0,5,0,10" Text="8080"/>

            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="Запустить сервер" Width="150" Margin="0,0,10,0" Click="StartServer_Click"/>
                <Button Content="Остановить сервер" Width="150" Click="StopServer_Click"/>
            </StackPanel>

            <TextBlock x:Name="ServerStatusText" 
                       Text="Сервер не запущен" 
                       Foreground="Gray" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
