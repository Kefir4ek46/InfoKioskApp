<UserControl x:Class="InfoKioskApp.Views.AdminTabs.BellSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#222">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Настройки расписания звонков"
                   FontSize="20"
                   Foreground="White"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- Таблица звонков -->
        <DataGrid x:Name="BellsGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Background="#333"
                  Foreground="White"
                  HeadersVisibility="Column"
                  RowBackground="#444"
                  AlternatingRowBackground="#3a3a3a"
                  BorderThickness="0"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding Number}" Width="50"/>
                <DataGridTextColumn Header="Начало" Binding="{Binding Start}" Width="*"/>
                <DataGridTextColumn Header="Конец" Binding="{Binding End}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0" >
            <Button Content="Добавить" Click="AddBell_Click" Width="100" Height="30" Background="#444" Foreground="White"/>
            <Button Content="Удалить" Click="RemoveBell_Click" Width="100" Height="30" Background="#444" Foreground="White"/>
            <Button Content="Сохранить" Click="SaveBells_Click" Width="100" Height="30" Background="#2a82da" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>
