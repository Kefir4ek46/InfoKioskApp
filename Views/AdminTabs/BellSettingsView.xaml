<UserControl x:Class="InfoKioskApp.Views.AdminTabs.BellSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#1C1C1C">

    <Border CornerRadius="12" Background="#262626" Padding="20" Margin="20" BorderBrush="#333" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Text="Настройки расписания звонков"
                       FontSize="22"
                       Foreground="White"
                       FontWeight="Bold"
                       Margin="0,0,0,15"/>

            <!-- Таблица звонков -->
            <DataGrid x:Name="BellsGrid"
                      Grid.Row="1"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      Background="#2E2E2E"
                      Foreground="White"
                      RowBackground="#3A3A3A"
                      AlternatingRowBackground="#353535"
                      BorderThickness="0"
                      RowHeight="32"
                      GridLinesVisibility="None"
                      SelectionMode="Single"
                      HorizontalGridLinesBrush="#444"
                      VerticalGridLinesBrush="#444">
                <DataGrid.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3E7DD1"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#2E2E2E"/>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="№" Binding="{Binding Number}" Width="50" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Начало" Binding="{Binding Start}" Width="*"/>
                    <DataGridTextColumn Header="Конец" Binding="{Binding End}" Width="*"/>
                    <DataGridTextColumn Header="Перемена (мин)" Binding="{Binding BreakAfter}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Нижняя панель -->
            <DockPanel Grid.Row="2" Margin="0,15,0,0">
                <!-- Левая часть: выбор параметров -->
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="10">
                    <TextBlock Text="Начало:" Foreground="White" VerticalAlignment="Center"/>
                    <TextBox x:Name="StartTimeBox" Text="08:00" Width="60" Margin="0,0,10,0"/>

                    <TextBlock Text="Длительность урока:" Foreground="White" VerticalAlignment="Center"/>
                    <ComboBox x:Name="LessonDurationCombo" Width="70" SelectedIndex="3">
                        <ComboBoxItem Content="30"/>
                        <ComboBoxItem Content="35"/>
                        <ComboBoxItem Content="40"/>
                        <ComboBoxItem Content="45"/>
                        <ComboBoxItem Content="50"/>
                    </ComboBox>

                    <Button Content="Пересчитать" Click="Recalculate_Click" Background="#444" Foreground="White" Margin="10,0,0,0"/>
                </StackPanel>

                <!-- Правая часть: действия -->
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right" Margin="10">
                    <Button Content="Добавить" Click="AddBell_Click" Width="100" Height="32" Background="#444" Foreground="White"/>
                    <Button Content="Удалить" Click="RemoveBell_Click" Width="100" Height="32" Background="#444" Foreground="White"/>
                    <Button Content="💾 Сохранить" Click="SaveBells_Click" Width="120" Height="32" Background="#2A82DA" Foreground="White"/>
                </StackPanel>
            </DockPanel>
        </Grid>
    </Border>
</UserControl>
