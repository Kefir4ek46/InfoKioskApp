<UserControl x:Class="InfoKioskApp.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#1E1E1E"
             d:DesignHeight="600" d:DesignWidth="800">

	<Grid Margin="20">
		<Border CornerRadius="15" Background="#2A2A2A" Padding="20" >
			<StackPanel>
				<TextBlock Text="Календарь событий"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="White"
                           Margin="0,0,0,15"/>

				<Calendar x:Name="MainCalendar"
                          Background="#333333"
                          Foreground="White"
                          BorderBrush="#444"
                          BorderThickness="1"
                          FontSize="16"/>

				<TextBlock x:Name="SelectedDateText"
                           Text="Выберите дату"
                           Foreground="LightGray"
                           FontSize="18"
                           Margin="0,20,0,0"
                           TextAlignment="Center"/>
			</StackPanel>
		</Border>
	</Grid>
</UserControl>
