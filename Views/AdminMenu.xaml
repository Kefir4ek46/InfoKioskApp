<Window x:Class="InfoKioskApp.Views.AdminMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Админ-панель"
        Topmost="True"
        WindowState="Maximized"
        
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <!-- Навигация -->
            <ColumnDefinition Width="*"/>
            <!-- Контент -->
        </Grid.ColumnDefinitions>

        <!-- Навигация -->
        <StackPanel Background="#252526" VerticalAlignment="Stretch" Margin="10">
            <TextBlock Text="Админ меню" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0 0 0 20"/>

            <Button Content="⚙ Настройки интерфейса" Style="{StaticResource MenuButtonStyle}" Click="InterfaceSettings_Click"/>
            <Button Content="📅 Расписания" Style="{StaticResource MenuButtonStyle}" Click="ScheduleSettings_Click"/>
            <Button Content="🔔 Расписание звонков" Style="{StaticResource MenuButtonStyle}" Click="BellSettings_Click"/>
            <Button Content="🌤 Погода" Style="{StaticResource MenuButtonStyle}" Click="WeatherSettings_Click"/>
            <Button Content="📆 Календарь" Style="{StaticResource MenuButtonStyle}" Click="CalendarSettings_Click"/>
            <Button Content="🌐 Удалённое подключение" Style="{StaticResource MenuButtonStyle}" Click="RemoteSettings_Click"/>
            <Button Content="🔐 Сменить PIN"  Style="{StaticResource MenuButtonStyle}"  Click="ChangePin_Click"/>


            <StackPanel VerticalAlignment="Bottom" Margin="0,20,0,0">
                <Button Content="🔒 Выйти" Style="{StaticResource MenuButtonStyle}" Click="Logout_Click"/>
                <Button Content="❌ Закрыть приложение" Style="{StaticResource MenuButtonStyle}" Click="Exit_Click"/>
                <Button Content="🗕 Свернуть" Style="{StaticResource MenuButtonStyle}" Click="Minimize_Click"/>
            </StackPanel>
        </StackPanel>

        <!-- Контент -->
        <Border Grid.Column="1" Margin="10" CornerRadius="8" Background="#2D2D30">
            <ContentControl x:Name="ContentArea"/>
        </Border>
    </Grid>
</Window>

