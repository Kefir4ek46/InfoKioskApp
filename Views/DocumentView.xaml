<UserControl x:Class="InfoKioskApp.Views.DocumentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#111">
        <StackPanel Margin="20">
            <TextBlock Text="📄 Документы" FontSize="26" Foreground="White" Margin="0,0,0,20"/>

            <ListBox x:Name="DocumentsList"
                     Background="#222"
                     Foreground="White"
                     BorderBrush="#444"
                     BorderThickness="1"
                     Height="400"
                     Margin="0,0,0,10"
                     SelectionChanged="DocumentsList_SelectionChanged"/>

            <Button Content="Открыть документ"
                    Click="OpenDocument_Click"
                    Width="200" Height="40"
                    HorizontalAlignment="Left"
                    Background="#333" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>

